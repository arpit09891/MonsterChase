#include"PlayerController.h"


Vector3 PlayerController::GetMovementDirectionFromUserInput() {
	/*printf("\n Press W to go up, A to go left, D to go right and S to go down \n");
	char gameChoice;
	gameChoice = _getch();

	switch (gameChoice)
	{
	case 'w':
	case 'W':
		return (Point2D(1, 0));


	case 's':
	case 'S':
		return (Point2D(-1, 0));

	case 'a':
	case 'A':
		return (Point2D(0, -1));
	case 'd':
	case 'D':
		return (Point2D(0, 1));

	default:
		printf("\n Please enter the valid value");*/
		return (Vector3());
	//}
}